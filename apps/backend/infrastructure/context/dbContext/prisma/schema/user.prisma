model User {
  id        String   @id @unique
  name      String
  lastName  String
  email     String
  password  String
  role      String
  mobile    String
  status    String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  orders        Order[]
  notifications Notification[]
  wishlists     WishList[]
}
