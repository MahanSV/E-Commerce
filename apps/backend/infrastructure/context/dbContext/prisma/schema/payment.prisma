model Payment {
  id                 String   @id @unique
  orderId            String
  cardName           String
  cardNumber         String
  cardExpirationDate DateTime
  cardCVV            String

  order Order @relation(fields: [orderId], references: [id])
}